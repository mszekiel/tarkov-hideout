{
  "name": "tarkov-calculator",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm i --prefix application && npm i --prefix backend &&  npm run build:prod --prefix application"
  },
  "dependencies": {
    "@nestjs/cli": "^6.13.2"
  }
}
